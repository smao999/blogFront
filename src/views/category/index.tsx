import React, { memo, useState } from 'react'
import MacDocker from "@/components/Docker";


const Category = memo(() => {
  
  return (
    <div>
      <MacDocker/>
    </div>
  )
})

export default Category